name: CI-Pipeline

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate prompts.json
        run: python3 -m json.tool prompts.json > /dev/null

      - run: docker build -t ai-prompt-library .
