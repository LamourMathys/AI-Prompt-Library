name: CI-Pipeline

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate prompt.json
        run: python3 -m json.tool prompt.json > /dev/null

      - run: docker build -t ai-prompt-library .
